import fs from "node:fs";
import path from "node:path";

const root = process.cwd();
const componentDir = path.join(root, "components/analytics");
const apiDir = path.join(root, "app/api/visit");
const layoutPath = path.join(root, "app/layout.tsx");
fs.mkdirSync(componentDir,{recursive:true}); fs.mkdirSync(apiDir,{recursive:true});
fs.writeFileSync(path.join(componentDir,"VisitorPing.tsx"), `'use client';
import { useEffect,useRef,useState } from 'react';
export function VisitorPing(){const[count,setCount]=useState<number|null>(null);const[pos,setPos]=useState({x:16,y:110});const drag=useRef<{dx:number;dy:number}|null>(null);useEffect(()=>{try{const s=localStorage.getItem('persone-royale-counter-pos');if(s)setPos(JSON.parse(s));}catch{}let stop=false;async function sync(){try{const key='persone-royale-visit-pushed';let visitorId=localStorage.getItem('persone-royale-visitor-id');if(!visitorId){visitorId=crypto.randomUUID?.()||Math.random().toString(36).slice(2);localStorage.setItem('persone-royale-visitor-id',visitorId)}const first=!sessionStorage.getItem(key);const r=await fetch('/api/visit',{method:first?'POST':'GET',headers:{'content-type':'application/json'},body:first?JSON.stringify({path:location.pathname,referrer:document.referrer||'',visitorId,visitKey:crypto.randomUUID?.()||Date.now().toString(36)}):undefined,cache:'no-store',keepalive:true});if(r.ok&&!stop){const d=await r.json().catch(()=>({}));if(typeof d.count==='number')setCount(d.count);if(first)sessionStorage.setItem(key,'1')}}catch{}}void sync();const t=setInterval(sync,5000);return()=>{stop=true;clearInterval(t)}},[]);function down(e:React.PointerEvent<HTMLDivElement>){const r=e.currentTarget.getBoundingClientRect();drag.current={dx:e.clientX-r.left,dy:e.clientY-r.top};e.currentTarget.setPointerCapture(e.pointerId)}function move(e:React.PointerEvent<HTMLDivElement>){if(!drag.current)return;setPos({x:Math.max(6,Math.min(innerWidth-126,e.clientX-drag.current.dx)),y:Math.max(6,Math.min(innerHeight-58,e.clientY-drag.current.dy))})}function up(){drag.current=null;try{localStorage.setItem('persone-royale-counter-pos',JSON.stringify(pos))}catch{}}return <div onPointerDown={down} onPointerMove={move} onPointerUp={up} onPointerCancel={up} style={{position:'fixed',left:pos.x,top:pos.y,zIndex:2147483000,touchAction:'none',userSelect:'none',cursor:'grab',minWidth:120,padding:'9px 12px',borderRadius:16,border:'1px solid rgba(234,190,83,.72)',background:'linear-gradient(145deg,rgba(18,15,10,.94),rgba(4,4,4,.94))',boxShadow:'0 8px 28px rgba(0,0,0,.45)',color:'#f6df9a',fontFamily:'system-ui,sans-serif',textAlign:'center'}} aria-label="Licznik wejść"><div style={{fontSize:9,letterSpacing:'1.6px',opacity:.72,fontWeight:700}}>WEJŚCIA</div><div style={{fontSize:21,lineHeight:'24px',fontWeight:900}}>{count===null?'—':count.toLocaleString('pl-PL')}</div></div>}
`);
fs.writeFileSync(path.join(apiDir,"route.ts"), `import {NextRequest,NextResponse} from 'next/server'; export const runtime='nodejs';export const dynamic='force-dynamic';const U='https://wmcgybrgnxeghvryqitt.supabase.co/functions/v1/visit-push';function bot(u:string){return /bot|crawler|spider|headless|lighthouse|pagespeed|preview|googlebot|bingbot|yandex|duckduckbot|semrush|ahrefs|uptimerobot/i.test(u)}async function call(a:string,r:NextRequest,b:any={}){const u=r.headers.get('user-agent')||'';const x=await fetch(U+'?action='+a,{method:'POST',headers:{'content-type':'application/json','user-agent':u},body:JSON.stringify(b),cache:'no-store'});const o=await x.json().catch(()=>({ok:x.ok}));return NextResponse.json(o,{status:x.ok?200:502,headers:{'cache-control':'no-store'}})}export async function GET(r:NextRequest){try{return await call('count',r)}catch{return NextResponse.json({ok:false},{status:502})}}export async function POST(r:NextRequest){const u=r.headers.get('user-agent')||'';if(!u||bot(u))return NextResponse.json({ok:true,ignored:true});let b:any={};try{b=await r.json()}catch{}try{return await call('visit',r,{path:typeof b.path==='string'?b.path.slice(0,500):'/',referrer:typeof b.referrer==='string'?b.referrer.slice(0,1000):'',visitorId:typeof b.visitorId==='string'?b.visitorId.slice(0,128):'',visitKey:typeof b.visitKey==='string'?b.visitKey.slice(0,128):''})}catch{return NextResponse.json({ok:false},{status:502})}}`);
let layout=fs.readFileSync(layoutPath,"utf8");if(!layout.includes('VisitorPing')){const ia='import "./globals.css";';if(!layout.includes(ia))throw new Error('layout import anchor missing');layout=layout.replace(ia,ia+'\nimport { VisitorPing } from "@/components/analytics/VisitorPing";');const body=/<body([^>]*)>/;if(!body.test(layout))throw new Error('body anchor missing');layout=layout.replace(body,'<body$1><VisitorPing />');fs.writeFileSync(layoutPath,layout)}
console.log('Installed Persone Royale visitor counter');
